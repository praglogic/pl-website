---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Company | Pragmatic Logic" description="Building emotionally intelligent AI that amplifies human compassion in healthcare. Meet the team creating the future of senior mental health care.">
  <Navigation />

  <!-- Hero Section -->
  <section class="about-hero">
    <div class="container">
      <div class="hero-content">
        <div class="company-logo">
          <img src="/images/pragmatic-logic-logo.png" alt="Pragmatic Logic - Human Ingenuity + AI Symbiosis" class="company-logo-img" />
        </div>

        <h1>The Centaur's Mind</h1>
        <p class="hero-subtitle">
          AI that amplifies human compassion, not replaces it
        </p>
      </div>
    </div>
  </section>

  <!-- Philosophy Section -->
  <section class="philosophy-section">
    <div class="container">
      <div class="philosophy-grid">
        <div class="philosophy-main">
          <h2>Why We Exist</h2>
          <p class="lead">
            Healthcare AI today excels at pattern recognition but lacks what truly heals: <strong>emotional intelligence</strong>. Patients need more than accurate diagnoses—they need to feel heard, understood, and cared for.
          </p>
          <p>
            Like the mythical centaur that combines human intelligence with raw strength, we unite human emotional wisdom with AI's computational power. This symbiotic relationship amplifies both human empathy and artificial intelligence.
          </p>
        </div>

        <div class="principles-card">
          <h3>Core Principles</h3>
          <div class="principle">
            <span class="principle-icon">🤝</span>
            <div>
              <h4>Symbiosis Over Replacement</h4>
              <p>Enhancing human capabilities</p>
            </div>
          </div>
          <div class="principle">
            <span class="principle-icon">❤️</span>
            <div>
              <h4>Emotional Intelligence First</h4>
              <p>Technical accuracy can't heal alone</p>
            </div>
          </div>
          <div class="principle">
            <span class="principle-icon">🎯</span>
            <div>
              <h4>Clinical-Grade Precision</h4>
              <p>Empathy paired with rigor</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Mission & Vision -->
  <section class="mission-section">
    <div class="container">
      <div class="mission-grid">
        <div class="mission-card">
          <h2>Mission</h2>
          <p>
            Create emotionally intelligent therapeutic AI that works in symbiosis with human caregivers, transforming senior mental health through the fusion of empathy and technology.
          </p>
        </div>

        <div class="vision-card">
          <h2>Vision</h2>
          <p>
            A future where every senior receives personalized, empathetic mental health support—where AI amplifies human compassion to prevent crises and restore dignity to aging.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Founder Section -->
  <section class="founder-section">
    <div class="container">
      <h2>Meet the Founder</h2>
      <div class="founder-content">
        <div class="founder-image">
          <img src="/images/aejaz-profile.jpg" alt="Aejaz Sheriff Quaraishi" class="profile-photo">
        </div>

        <div class="founder-info">
          <h3>Aejaz Sheriff Quaraishi</h3>
          <div class="founder-title">Founder & Principal Architect</div>

          <p class="founder-bio">
            28 years building regulated, mission-critical platforms across healthcare, AI, and enterprise systems. Expert in clinical data interoperability (HL7 FHIR), multi-agent AI architectures, and HIPAA-compliant systems.
          </p>

          <div class="founder-highlights">
            <div class="highlight-item">
              <strong>Built:</strong> 26-microservice clinical-grade AI platform
            </div>
            <div class="highlight-item">
              <strong>Achieved:</strong> 100% recall, zero false negatives in crisis detection
            </div>
            <div class="highlight-item">
              <strong>Delivered:</strong> 128 days ahead of plan, saving $1.75M–$2.55M
            </div>
          </div>

          <blockquote class="founder-quote">
            "After three decades in healthcare technology, I recognized a critical gap: AI that could process clinical data but couldn't truly understand the human experience. Pragmatic Logic represents my vision of The Centaur's Mind—where artificial intelligence amplifies human empathy rather than replacing it."
          </blockquote>

          <div class="founder-contact">
            <a href="mailto:contact@pragmaticlogic.ai">contact@pragmaticlogic.ai</a>
            <span>•</span>
            <a href="https://www.linkedin.com/in/asheriff/" target="_blank">LinkedIn</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Values -->
  <section class="values-section">
    <div class="container">
      <h2>Our Values</h2>
      <div class="values-grid">
        <div class="value-card">
          <div class="value-icon">🧠</div>
          <h3>Clinical Excellence</h3>
          <p>Every decision grounded in peer-reviewed research</p>
        </div>

        <div class="value-card">
          <div class="value-icon">💝</div>
          <h3>Human-Centered Design</h3>
          <p>Technology should serve humanity</p>
        </div>

        <div class="value-card">
          <div class="value-icon">🔒</div>
          <h3>Privacy & Security</h3>
          <p>Healthcare data deserves the highest protection</p>
        </div>

        <div class="value-card">
          <div class="value-icon">⚖️</div>
          <h3>Ethical AI</h3>
          <p>Transparent, fair, and accountable</p>
        </div>

        <div class="value-card">
          <div class="value-icon">🌟</div>
          <h3>Continuous Innovation</h3>
          <p>The pursuit of better outcomes never stops</p>
        </div>

        <div class="value-card">
          <div class="value-icon">🤲</div>
          <h3>Compassionate Care</h3>
          <p>Every interaction should add value</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="about-cta">
    <div class="container">
      <div class="cta-content">
        <h2>Join Us in Building the Future</h2>
        <p>
          Looking for investors, partners, and team members who share our vision of emotionally intelligent healthcare AI.
        </p>

        <div class="cta-actions">
          <a href="/demo" class="btn-primary-large">See Our Vision in Action</a>
          <a href="mailto:contact@pragmaticlogic.ai" class="btn-secondary-large">Get in Touch</a>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  /* Hero Section */
  .about-hero {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    padding: var(--space-24) 0 var(--space-20);
    position: relative;
    overflow: hidden;
  }

  .about-hero::before {
    content: '';
    position: absolute;
    top: -20%;
    left: -10%;
    width: 60%;
    height: 140%;
    background: radial-gradient(circle at 50% 50%, rgba(255,255,255,0.15) 0%, transparent 60%);
    pointer-events: none;
    animation: float 25s ease-in-out infinite;
  }

  .about-hero::after {
    content: '';
    position: absolute;
    bottom: -20%;
    right: -10%;
    width: 50%;
    height: 120%;
    background: radial-gradient(circle at 50% 50%, rgba(16, 185, 129, 0.2) 0%, transparent 60%);
    pointer-events: none;
    animation: float 30s ease-in-out infinite reverse;
  }

  @keyframes float {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    50% {
      transform: translate(-20px, -20px) scale(1.1);
    }
  }

  .hero-content {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .company-logo {
    margin-bottom: var(--space-8);
  }

  .company-logo-img {
    max-width: 500px;
    width: 100%;
    height: auto;
    filter: brightness(0) invert(1);
  }

  .about-hero h1 {
    font-size: var(--font-size-5xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
    letter-spacing: -0.03em;
  }

  .hero-subtitle {
    font-size: var(--font-size-2xl);
    opacity: 0.95;
    font-weight: 400;
    line-height: 1.5;
  }

  /* Philosophy Section */
  .philosophy-section {
    padding: var(--space-24) 0;
    background: white;
  }

  .philosophy-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--space-16);
    align-items: start;
  }

  .philosophy-main h2 {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-6);
    color: var(--color-text);
    letter-spacing: -0.02em;
  }

  .lead {
    font-size: var(--font-size-xl);
    color: var(--color-text);
    margin-bottom: var(--space-6);
    line-height: 1.7;
  }

  .philosophy-main p {
    font-size: var(--font-size-lg);
    color: var(--color-text-light);
    line-height: 1.75;
    margin-bottom: var(--space-4);
    max-width: 100%;
  }

  .principles-card {
    position: sticky;
    top: var(--space-8);
    background: linear-gradient(135deg, var(--color-background-soft), var(--color-background));
    padding: var(--space-8);
    border-radius: var(--radius-2xl);
    border: 1px solid var(--color-border-light);
    box-shadow: var(--shadow-md);
  }

  .principles-card h3 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    margin-bottom: var(--space-6);
    color: var(--color-text);
  }

  .principle {
    display: flex;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .principle:last-child {
    margin-bottom: 0;
  }

  .principle-icon {
    font-size: 2rem;
    flex-shrink: 0;
  }

  .principle h4 {
    font-size: var(--font-size-base);
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: var(--space-1);
  }

  .principle p {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    margin: 0;
    max-width: 100%;
  }

  /* Mission Section */
  .mission-section {
    padding: var(--space-20) 0;
    background: var(--color-background-soft);
  }

  .mission-section h2 {
    text-align: center;
    font-size: var(--font-size-3xl);
    font-weight: 700;
    margin-bottom: var(--space-12);
    color: var(--color-text);
  }

  .mission-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-8);
  }

  .mission-card, .vision-card {
    padding: var(--space-8);
    border-radius: var(--radius-2xl);
    background: white;
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
  }

  .mission-card:hover, .vision-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .mission-card h2, .vision-card h2 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
    color: var(--color-primary);
  }

  .mission-card p, .vision-card p {
    color: var(--color-text-light);
    line-height: 1.75;
    font-size: var(--font-size-lg);
    margin: 0;
    max-width: 100%;
  }

  /* Founder Section */
  .founder-section {
    padding: var(--space-24) 0;
    background: white;
  }

  .founder-section > h2 {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    text-align: center;
    margin-bottom: var(--space-12);
    color: var(--color-text);
  }

  .founder-content {
    display: grid;
    grid-template-columns: 300px 1fr;
    gap: var(--space-12);
    align-items: start;
  }

  .founder-image {
    text-align: center;
  }

  .profile-photo {
    width: 300px;
    height: 300px;
    border-radius: 50%;
    object-fit: cover;
    box-shadow: var(--shadow-xl);
    border: 4px solid white;
    transition: transform 0.3s ease;
  }

  .profile-photo:hover {
    transform: scale(1.05);
  }

  .founder-info h3 {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    margin-bottom: var(--space-2);
    color: var(--color-text);
  }

  .founder-title {
    font-size: var(--font-size-xl);
    color: var(--color-primary);
    font-weight: 600;
    margin-bottom: var(--space-6);
  }

  .founder-bio {
    color: var(--color-text-light);
    line-height: 1.75;
    margin-bottom: var(--space-6);
    font-size: var(--font-size-lg);
    max-width: 100%;
  }

  .founder-highlights {
    display: grid;
    gap: var(--space-3);
    margin-bottom: var(--space-6);
  }

  .highlight-item {
    padding: var(--space-3) var(--space-4);
    background: var(--color-background-soft);
    border-radius: var(--radius-lg);
    border-left: 3px solid var(--color-secondary);
    font-size: var(--font-size-base);
  }

  .highlight-item strong {
    color: var(--color-text);
  }

  .founder-quote {
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.05), rgba(16, 185, 129, 0.05));
    border-left: 4px solid var(--color-primary);
    padding: var(--space-6);
    border-radius: var(--radius-xl);
    font-style: italic;
    color: var(--color-text);
    line-height: 1.75;
    margin-bottom: var(--space-6);
  }

  .founder-contact {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    font-size: var(--font-size-base);
  }

  .founder-contact a {
    color: var(--color-primary);
    text-decoration: none;
    font-weight: 600;
  }

  .founder-contact a:hover {
    text-decoration: underline;
  }

  /* Values Section */
  .values-section {
    padding: var(--space-20) 0;
    background: var(--color-background-soft);
  }

  .values-section h2 {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    text-align: center;
    margin-bottom: var(--space-12);
    color: var(--color-text);
  }

  .values-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  .value-card {
    background: white;
    padding: var(--space-8);
    border-radius: var(--radius-2xl);
    text-align: center;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    border: 1px solid var(--color-border-light);
    box-shadow: var(--shadow-sm);
  }

  .value-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-xl);
    border-color: var(--color-primary);
  }

  .value-icon {
    font-size: 3rem;
    margin-bottom: var(--space-4);
    display: inline-block;
    transition: transform 0.4s ease;
  }

  .value-card:hover .value-icon {
    transform: scale(1.2) rotate(5deg);
  }

  .value-card h3 {
    font-size: var(--font-size-xl);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--color-text);
  }

  .value-card p {
    color: var(--color-text-light);
    line-height: 1.75;
    margin: 0;
    max-width: 100%;
  }

  /* CTA Section */
  .about-cta {
    padding: var(--space-20) 0;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .about-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 70% 30%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
    pointer-events: none;
  }

  .cta-content {
    position: relative;
    z-index: 1;
  }

  .about-cta h2 {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
  }

  .about-cta p {
    font-size: var(--font-size-xl);
    margin-bottom: var(--space-8);
    opacity: 0.95;
    max-width: 100%;
  }

  .cta-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
  }

  .btn-primary-large, .btn-secondary-large {
    padding: var(--space-4) var(--space-8);
    border-radius: var(--radius-xl);
    font-weight: 600;
    font-size: var(--font-size-lg);
    text-decoration: none;
    transition: all 0.3s ease;
    border: 2px solid transparent;
  }

  .btn-primary-large {
    background: white;
    color: var(--color-primary);
  }

  .btn-primary-large:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .btn-secondary-large {
    background: transparent;
    color: white;
    border-color: rgba(255, 255, 255, 0.5);
  }

  .btn-secondary-large:hover {
    border-color: white;
    background: rgba(255, 255, 255, 0.1);
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .philosophy-grid {
      grid-template-columns: 1fr;
      gap: var(--space-12);
    }

    .principles-card {
      position: relative;
      top: 0;
    }

    .mission-grid {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .founder-content {
      grid-template-columns: 1fr;
      gap: var(--space-8);
      text-align: center;
    }

    .founder-image {
      justify-self: center;
    }

    .values-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .about-hero {
      padding: var(--space-16) 0;
    }

    .about-hero h1 {
      font-size: var(--font-size-4xl);
    }

    .hero-subtitle {
      font-size: var(--font-size-xl);
    }

    .values-grid {
      grid-template-columns: 1fr;
    }

    .founder-info h3 {
      font-size: var(--font-size-2xl);
    }
  }

  @media (max-width: 640px) {
    .company-logo-img {
      max-width: 100%;
    }

    .about-hero h1 {
      font-size: var(--font-size-3xl);
    }

    .hero-subtitle {
      font-size: var(--font-size-lg);
    }

    .cta-actions {
      flex-direction: column;
      width: 100%;
    }

    .btn-primary-large,
    .btn-secondary-large {
      width: 100%;
    }

    .profile-photo {
      width: 200px;
      height: 200px;
    }

    .about-cta h2 {
      font-size: var(--font-size-2xl);
    }

    .about-cta p {
      font-size: var(--font-size-base);
    }
  }
</style>
