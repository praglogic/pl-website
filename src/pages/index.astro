---
const site = {
    title: "Pragmatic Logic — Coming Soon",
    description:
        "We’re building healthcare-grade AI with empathy. Our flagship product, Lilo, is a therapeutic emotional agent for seniors. Meanwhile, reach us at contact@pragmaticlogic.ai.",
    url: "https://pragmaticlogic.ai",
    email: "contact@pragmaticlogic.ai",
};
---
<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{site.title}</title>
    <meta name="description" content={site.description} />
    <link rel="icon" href="/favicon.svg" />
    <!-- Open Graph -->
    <meta property="og:title" content={site.title} />
    <meta property="og:description" content={site.description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={site.url} />
    <meta property="og:image" content="/og.png" />
    <!-- Robots -->
    <meta name="robots" content="index,follow" />
    <style>
        :root{
            /* Empathetic, calm palette with a subtle, optimistic accent */
            --bg-0: #0b0f14;
            --bg-1: #0f1720;
            --card: rgba(255,255,255,0.06);
            --glow: 180deg 80% 60%;
            --text: #e8eef7;
            --muted: #a8b3c7;
            --accent: #77e2c6; /* mint-teal: warm, reassuring */
            --accent-2: #8ea6ff; /* soft periwinkle for depth */
            --max: 68rem;
            --radius: 18px;
        }

        /* Futuristic grid backdrop with soft glows */
        body{
            margin:0; min-height:100svh; color:var(--text);
            font:16px/1.6 ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Inter, Arial;
            background:
                    radial-gradient(1200px 600px at 20% -10%, #1d2540 10%, transparent 60%),
                    radial-gradient(900px 500px at 100% 10%, #123b34 10%, transparent 60%),
                    linear-gradient(180deg, var(--bg-0), var(--bg-1));
            display:grid; place-items:center; padding:2rem;
        }
        .grid {
            position:fixed; inset:0; pointer-events:none; opacity:.25;
            background-image:
                    linear-gradient(to right, rgba(255,255,255,.06) 1px, transparent 1px),
                    linear-gradient(to bottom, rgba(255,255,255,.06) 1px, transparent 1px);
            background-size: 48px 48px, 48px 48px;
            mask-image: radial-gradient(60% 60% at 50% 40%, #000 60%, transparent 100%);
        }
        .wrap{ width:100%; max-width:var(--max); }
        .card{
            backdrop-filter: blur(12px);
            background: var(--card);
            border: 1px solid rgba(255,255,255,.08);
            border-radius: var(--radius);
            padding: clamp(1.25rem, 3vw, 2rem);
            box-shadow:
                    0 0 0 1px rgba(255,255,255,.03) inset,
                    0 40px 80px rgba(0,0,0,.35),
                    0 0 80px 20px color-mix(in hsl, hsl(var(--glow)) 40%, transparent);
        }

        header{ display:flex; align-items:center; gap:.75rem; margin-bottom:1rem; }
        .logo{
            width:40px; height:40px; border-radius:12px;
            background: radial-gradient(120% 120% at 20% 20%, var(--accent), var(--accent-2));
            display:grid; place-items:center; color:#0b0f14; font-weight:700;
            box-shadow: 0 8px 24px rgba(119,226,198,.35);
        }
        h1{ margin:.25rem 0 0; font-size:clamp(1.75rem, 4vw, 2.5rem); line-height:1.2; }
        p.lead{ color:var(--muted); margin:.5rem 0 1.25rem; font-size:clamp(1rem, 2.2vw, 1.15rem); }

        .chips{ display:flex; flex-wrap:wrap; gap:.5rem; margin:.5rem 0 1.25rem; }
        .chip{
            background: rgba(255,255,255,.06); color:var(--text);
            border: 1px solid rgba(255,255,255,.1); border-radius:999px;
            padding:.35rem .7rem; font-size:.9rem;
        }

        .cta-row{ display:flex; flex-wrap:wrap; gap:.65rem; margin-top:.5rem; }
        .btn{
            appearance:none; border-radius:999px; padding:.75rem 1rem; font-weight:600;
            text-decoration:none; display:inline-flex; align-items:center; gap:.5rem;
            border:1px solid rgba(255,255,255,.18); color:#0b0f14; background: var(--accent);
            box-shadow: 0 8px 24px rgba(119,226,198,.3);
        }
        .btn.secondary{
            background: transparent; color:var(--text);
            border:1px solid rgba(255,255,255,.22);
        }
        .note{
            margin-top:1rem; font-size:.95rem; color:var(--muted);
        }

        footer{
            margin-top:1.25rem; color:var(--muted); font-size:.9rem; display:flex; gap:.75rem; flex-wrap:wrap;
        }
        footer a{ color:var(--text); opacity:.9; text-decoration:underline; text-underline-offset:3px; }

        /* Motion that respects user preference */
        @media (prefers-reduced-motion: no-preference){
            .card{ animation: rise .9s ease both }
            @keyframes rise{ from{ transform: translateY(10px); opacity:0 } to{ transform: translateY(0); opacity:1 } }
        }

        /* AA “easy read” toggle (no cookies; just CSS class) */
        .access-toggle{ position:fixed; top:14px; right:14px; z-index:3 }
        .switch{ background: rgba(255,255,255,.08); border:1px solid rgba(255,255,255,.25); color:var(--text);
            border-radius:999px; padding:.35rem .7rem; cursor:pointer; font-size:.9rem; }
        .easy body{ font-size:18px }
        .easy .card{ border-color: rgba(255,255,255,.3) }
        .easy a.btn.secondary{ border-color: rgba(255,255,255,.4) }
        .easy :focus-visible{ outline:3px solid var(--accent) !important; outline-offset:2px }

    </style>
    <script>
        // Persist "Easy-read" preference locally (no cookies)
        const key = "pl_easy_read";
        const set = (on)=>{ on ? document.documentElement.classList.add("easy") : document.documentElement.classList.remove("easy"); localStorage.setItem(key, on ? "1":"0"); }
        document.addEventListener("DOMContentLoaded", ()=>{
            set(localStorage.getItem(key) === "1");
            const btn = document.getElementById("easy-toggle");
            btn?.addEventListener("click", ()=> set(!document.documentElement.classList.contains("easy")));
        });
    </script>
</head>
<body>
<div class="grid" aria-hidden="true"></div>

<div class="access-toggle">
    <button id="easy-toggle" class="switch" aria-pressed="false" aria-label="Toggle easy-read mode">
        Easy-read Mode
    </button>
</div>

<main class="wrap">
    <section class="card" aria-labelledby="heading">
        <header>
            <div class="logo" aria-hidden="true">PL</div>
            <div>
                <h1 id="heading">Pragmatic Logic</h1>
                <p class="lead">Healthcare-grade AI, delivered <em>with empathy</em>. Website coming soon.</p>
            </div>
        </header>

        <div class="chips" role="list">
            <span class="chip" role="listitem">Consulting: Architecture · AI pilots</span>
            <span class="chip" role="listitem">HIPAA-aware practices</span>
            <span class="chip" role="listitem">Flagship: <strong>Lilo</strong> for seniors</span>
        </div>

        <div class="cta-row">
            <a class="btn" href={"mailto:" + site.email} aria-label="Email us at contact@pragmaticlogic.ai">
                ✉️ Contact us
            </a>
            <!-- If you want Calendly now, replace the # with your link -->
            <a class="btn secondary" href="#" aria-label="Book a call on Calendly (opens in new tab)" target="_blank" rel="noopener">
                Book a call
            </a>
        </div>

        <p class="note">Please don’t include medical details/PHI in messages.</p>

        <footer>
            <span>© {new Date().getFullYear()} Pragmatic Logic LLC</span>
            <span> · </span>
            <a
                    href={`mailto:${"contact"}@${"pragmaticlogic.ai"}`}
            >
                {site.email}
            </a>

        </footer>
    </section>
</main>
</body>
</html>
