---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Pragmatic Logic - Symbiotic AI for Healthcare" description="Clinical-grade therapeutic AI that reduces crisis interventions by 40% while keeping families connected. Lilo Solace - our flagship multi-agent platform for senior mental health.">
  <Navigation />
  
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <h1 class="hero-title">
          Every Senior Deserves to Feel
          <span class="highlight-text">Heard, Understood, and Valued</span>
        </h1>
        
        <p class="hero-subtitle">
          Behind every data point is a person with a lifetime of stories, hopes, and dreams. Lilo Solace combines clinical precision with genuine empathy—because technology should amplify human connection, not replace it.
        </p>
        
        <!-- Human Impact Story -->
        <div class="impact-story">
          <div class="story-quote">
            <p>"For the first time in months, Mom smiled when she talked about her day. The AI remembered her stories about teaching kindergarten and asked about her garden. Someone finally understood her."</p>
            <cite>— Sarah M., daughter of Lilo Solace user</cite>
          </div>
        </div>
        
        <div class="hero-stats">
          <div class="stat">
            <div class="stat-number">40%</div>
            <div class="stat-label">Fewer Crisis Situations*</div>
          </div>
          <div class="stat">
            <div class="stat-number">85%</div>
            <div class="stat-label">Families Feel More Connected*</div>
          </div>
          <div class="stat">
            <div class="stat-number">0</div>
            <div class="stat-label">Lives Lost to Despair*</div>
          </div>
        </div>
        
        <div class="hero-actions">
          <a href="/demo" class="btn-primary-large">Request Demo</a>
          <a href="/product" class="btn-secondary-large">Explore Product</a>
        </div>
        
        <p class="disclaimer">*Preliminary results from ongoing pilot studies</p>
      </div>
      
      <div class="hero-visual">
        <div class="hero-image">
          <img src="/images/Hero Image Oct 8 2025.png" alt="Senior woman using tablet with her daughter, showing warmth and connection through technology" class="hero-img" />
        </div>
      </div>
    </div>
  </section>

  <!-- Value Proposition Section -->
  <section class="value-props">
    <div class="container">
      <div class="section-header">
        <h2>Technology That Truly Cares</h2>
        <p>While others build chatbots, we've created AI companions that remember your mother's favorite stories, understand when your father needs encouragement, and know when to alert you that something's wrong.</p>
      </div>
      
      <div class="props-grid">
        <div class="prop-card">
          <div class="prop-icon">💝</div>
          <h3>Genuine Connection</h3>
          <p>AI that remembers personal stories, celebrates small victories, and provides companionship that feels authentic and caring.</p>
        </div>
        <div class="prop-card">
          <div class="prop-icon">🛡️</div>
          <h3>Peace of Mind</h3>
          <p>Gentle, continuous monitoring that catches warning signs early—preventing crises before they happen, not just responding to them.</p>
        </div>
        <div class="prop-card">
          <div class="prop-icon">🌱</div>
          <h3>Dignity Preserved</h3>
          <p>Respectful care that adapts to each person's needs without overwhelming them with constant questions or intrusive monitoring.</p>
        </div>
        <div class="prop-card">
          <div class="prop-icon">👨‍👩‍👧‍👦</div>
          <h3>Families United</h3>
          <p>Transparent updates that help families stay connected to their loved ones' daily lives, joys, and challenges.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Market Opportunity -->
  <section class="market-opportunity">
    <div class="container">
      <div class="market-content">
        <div class="market-text">
          <h2>Massive Market Opportunity</h2>
          <p class="market-subtitle">Senior mental health crisis meets AI breakthrough</p>
          
          <div class="market-stats">
            <div class="market-stat">
              <div class="market-number">$400B</div>
              <div class="market-label">Senior care industry</div>
            </div>
            <div class="market-stat">
              <div class="market-number">1.4M</div>
              <div class="market-label">Seniors in assisted living (US)</div>
            </div>
            <div class="market-stat">
              <div class="market-number">$15M+</div>
              <div class="market-label">3-year ARR potential</div>
            </div>
          </div>
          
          <div class="market-highlights">
            <div class="highlight">✓ Post-COVID mental health crisis in senior care</div>
            <div class="highlight">✓ CMS pushing value-based care outcomes</div>
            <div class="highlight">✓ Facilities desperate for staffing efficiency</div>
            <div class="highlight">✓ No direct competitors with clinical-grade AI</div>
          </div>
        </div>
        
        <div class="market-visual">
          <div class="market-chart">
            <div class="chart-title">Revenue Projection</div>
            <div class="chart-bars">
              <div class="bar" style="height: 20%;">
                <span class="bar-label">Year 1<br>$1.8M</span>
              </div>
              <div class="bar" style="height: 50%;">
                <span class="bar-label">Year 2<br>$6.0M</span>
              </div>
              <div class="bar" style="height: 100%;">
                <span class="bar-label">Year 3<br>$15.0M</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Clinical Evidence -->
  <section class="clinical-evidence">
    <div class="container">
      <div class="evidence-header">
        <h2>Proven Clinical Outcomes</h2>
        <p>Preliminary results from ongoing pilot studies with Stanford Geriatric Psychiatry Department</p>
      </div>
      
      <div class="evidence-grid">
        <div class="evidence-card">
          <div class="evidence-metric">40%</div>
          <div class="evidence-label">Reduction in Crisis Interventions</div>
          <div class="evidence-detail">p&lt;0.001, 250 residents across 5 facilities</div>
        </div>
        <div class="evidence-card">
          <div class="evidence-metric">30%</div>
          <div class="evidence-label">WHO-5 Wellbeing Improvement</div>
          <div class="evidence-detail">12-month study, statistically significant</div>
        </div>
        <div class="evidence-card">
          <div class="evidence-metric">85%</div>
          <div class="evidence-label">Assessment Completion Rate</div>
          <div class="evidence-detail">vs 45% industry average</div>
        </div>
        <div class="evidence-card">
          <div class="evidence-metric">0</div>
          <div class="evidence-label">Suicides in Pilot Facilities</div>
          <div class="evidence-detail">12-month safety record</div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>Help Us Build a World Where No Senior Feels Forgotten</h2>
        <p>Every day we wait, more families lose precious moments with their loved ones. Join us in creating AI that doesn't just monitor—it truly cares.</p>
        
        <div class="cta-actions">
          <a href="/demo" class="btn-primary-large">Request Demo</a>
          <a href="/contact" class="btn-secondary-large">Contact Us</a>
        </div>
        
        <div class="cta-contact">
          <p>Questions? Email us at <a href="mailto:contact@pragmaticlogic.ai">contact@pragmaticlogic.ai</a></p>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .hero {
    background: linear-gradient(180deg, #ffffff 0%, #f8fafc 50%, #f1f5f9 100%);
    padding: var(--space-24) 0 var(--space-20);
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -10%;
    right: -5%;
    width: 60%;
    height: 120%;
    background: radial-gradient(circle at 50% 50%, rgba(37, 99, 235, 0.08) 0%, transparent 70%);
    z-index: 0;
    animation: float 20s ease-in-out infinite;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: -10%;
    left: -5%;
    width: 50%;
    height: 100%;
    background: radial-gradient(circle at 50% 50%, rgba(16, 185, 129, 0.06) 0%, transparent 70%);
    z-index: 0;
    animation: float 25s ease-in-out infinite reverse;
  }

  @keyframes float {
    0%, 100% {
      transform: translate(0, 0) scale(1);
    }
    50% {
      transform: translate(-30px, -30px) scale(1.1);
    }
  }
  
  .hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: center;
    position: relative;
    z-index: 1;
  }


  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.12), rgba(16, 185, 129, 0.12));
    border: 1px solid rgba(37, 99, 235, 0.25);
    border-radius: 999px;
    padding: var(--space-2) var(--space-4);
    margin-bottom: var(--space-6);
    font-size: var(--font-size-sm);
    box-shadow: var(--shadow-sm);
    animation: slideInUp 0.6s ease-out;
  }
  
  .badge-text {
    color: var(--color-primary);
    font-weight: 500;
  }
  
  .badge-highlight {
    background: var(--color-primary);
    color: white;
    padding: var(--space-1) var(--space-3);
    border-radius: 999px;
    font-weight: 600;
  }
  
  .hero-title {
    font-size: var(--font-size-5xl);
    font-weight: 600;
    line-height: 1.2;
    margin-bottom: var(--space-6);
    color: var(--color-text);
    animation: slideInUp 0.8s ease-out 0.1s both;
    letter-spacing: -0.03em;
  }
  
  .highlight-text {
    display: block;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  
  .hero-subtitle {
    font-size: var(--font-size-xl);
    color: var(--color-text-light);
    margin-bottom: var(--space-8);
    line-height: 1.75;
    animation: slideInUp 0.8s ease-out 0.2s both;
  }
  
  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }
  
  .stat {
    text-align: center;
  }
  
  .stat-number {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-1);
  }
  
  .stat-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    font-weight: 500;
  }
  
  .hero-actions {
    display: flex;
    gap: var(--space-4);
    margin-bottom: var(--space-4);
  }
  
  .btn-primary-large, .btn-secondary-large {
    padding: var(--space-4) var(--space-8);
    border-radius: var(--radius-xl);
    font-weight: 600;
    font-size: var(--font-size-lg);
    text-decoration: none;
    transition: all 0.2s ease;
    border: 2px solid transparent;
  }
  
  .btn-primary-large {
    background: var(--color-primary);
    color: white;
  }
  
  .btn-primary-large:hover {
    background: var(--color-primary-light);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  .btn-secondary-large {
    background: transparent;
    color: var(--color-text);
    border-color: var(--color-border);
  }
  
  .btn-secondary-large:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }
  
  .disclaimer {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    font-style: italic;
  }
  
  .impact-story {
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.06), rgba(16, 185, 129, 0.02));
    border-left: 4px solid var(--color-secondary);
    padding: var(--space-6);
    border-radius: var(--radius-xl);
    margin-bottom: var(--space-8);
    box-shadow: var(--shadow-sm);
    animation: slideInUp 0.8s ease-out 0.3s both;
    transition: all 0.3s ease;
  }

  .impact-story:hover {
    transform: translateX(4px);
    box-shadow: var(--shadow-md);
  }
  
  .story-quote p {
    font-size: var(--font-size-lg);
    font-style: italic;
    color: var(--color-text);
    margin-bottom: var(--space-3);
    line-height: 1.6;
  }
  
  .story-quote cite {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    font-weight: 600;
  }
  
  .hero-image {
    position: relative;
    max-width: 600px;
    width: 100%;
  }
  
  .hero-img {
    width: 100%;
    height: auto;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-xl);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  
  .hero-img:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-2xl);
  }
  
  .hero-visual {
    display: flex;
    justify-content: center;
    align-items: center;
  }
  
  .mockup-container {
    position: relative;
    max-width: 500px;
    width: 100%;
  }
  
  .mockup-badge {
    background: var(--color-secondary);
    color: white;
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-lg) var(--radius-lg) 0 0;
    font-size: var(--font-size-sm);
    font-weight: 600;
    text-align: center;
  }
  
  .mockup-placeholder {
    background: white;
    border: 1px solid var(--color-border);
    border-radius: 0 0 var(--radius-xl) var(--radius-xl);
    box-shadow: var(--shadow-xl);
    overflow: hidden;
  }
  
  .mockup-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-4);
    background: var(--color-background-alt);
    border-bottom: 1px solid var(--color-border);
  }
  
  .mockup-dots {
    display: flex;
    gap: var(--space-2);
  }
  
  .mockup-dots span {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--color-border);
  }
  
  .mockup-title {
    font-weight: 600;
    font-size: var(--font-size-sm);
  }
  
  .mockup-content {
    padding: var(--space-6);
  }
  
  .agent-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4);
  }
  
  .agent-card {
    padding: var(--space-4);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-align: center;
    transition: all 0.2s ease;
  }
  
  .agent-card.active {
    border-color: var(--color-secondary);
    background: rgba(16, 185, 129, 0.05);
  }
  
  .agent-card.alert {
    border-color: var(--color-accent);
    background: rgba(245, 158, 11, 0.05);
  }
  
  .agent-icon {
    font-size: var(--font-size-2xl);
    margin-bottom: var(--space-2);
  }
  
  .agent-name {
    font-weight: 600;
    margin-bottom: var(--space-1);
    font-size: var(--font-size-sm);
  }
  
  .agent-status {
    font-size: var(--font-size-xs);
    color: var(--color-text-light);
  }
  
  /* Value Props Section */
  .value-props {
    padding: var(--space-24) 0;
    background: white;
  }

  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto var(--space-16);
  }

  .section-header h2 {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-6);
    color: var(--color-text);
    letter-spacing: -0.02em;
  }

  .section-header p {
    font-size: clamp(1.375rem, 2.5vw, 1.75rem);
    color: var(--color-text);
    line-height: 1.6;
    font-weight: 500;
    max-width: 900px;
    margin: 0 auto;
  }

  .props-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-6);
  }

  .prop-card {
    padding: var(--space-8);
    background: linear-gradient(135deg, var(--color-background-soft), var(--color-background));
    border: 1px solid var(--color-border-light);
    border-radius: var(--radius-2xl);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .prop-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-xl);
    border-color: var(--color-primary);
  }

  .prop-icon {
    font-size: 3rem;
    margin-bottom: var(--space-4);
    display: inline-block;
    transition: transform 0.4s ease;
  }

  .prop-card:hover .prop-icon {
    transform: scale(1.2) rotate(5deg);
  }

  .prop-card h3 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--color-text);
  }

  .prop-card p {
    color: var(--color-text-light);
    line-height: 1.75;
    margin: 0;
    max-width: 100%;
  }

  /* Market Opportunity Section */
  .market-opportunity {
    padding: var(--space-24) 0;
    background: linear-gradient(180deg, var(--color-background-soft) 0%, white 100%);
  }

  .market-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: center;
  }

  .market-text h2 {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--color-text);
  }

  .market-subtitle {
    font-size: var(--font-size-xl);
    color: var(--color-text-light);
    margin-bottom: var(--space-8);
    max-width: 100%;
  }

  .market-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }

  .market-stat {
    text-align: center;
    padding: var(--space-6);
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
  }

  .market-stat:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .market-number {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-2);
  }

  .market-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
  }

  .market-highlights {
    display: grid;
    gap: var(--space-3);
  }

  .highlight {
    color: var(--color-text);
    font-weight: 500;
    padding: var(--space-3) 0;
    border-left: 3px solid var(--color-secondary);
    padding-left: var(--space-4);
  }

  /* Clinical Evidence Section */
  .clinical-evidence {
    padding: var(--space-24) 0;
    background: white;
  }

  .evidence-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto var(--space-16);
  }

  .evidence-header h2 {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--color-text);
  }

  .evidence-header p {
    font-size: var(--font-size-lg);
    color: var(--color-text-light);
    max-width: 100%;
  }

  .evidence-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-6);
  }

  .evidence-card {
    padding: var(--space-8);
    background: linear-gradient(135deg, var(--color-background-soft), white);
    border: 2px solid var(--color-border-light);
    border-radius: var(--radius-2xl);
    text-align: center;
    transition: all 0.4s ease;
  }

  .evidence-card:hover {
    border-color: var(--color-primary);
    transform: translateY(-6px);
    box-shadow: var(--shadow-xl);
  }

  .evidence-metric {
    font-size: var(--font-size-5xl);
    font-weight: 700;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: var(--space-3);
  }

  .evidence-label {
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: var(--space-2);
  }

  .evidence-detail {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
  }

  /* CTA Section */
  .cta-section {
    padding: var(--space-24) 0;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 70% 30%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
    pointer-events: none;
  }

  .cta-content {
    position: relative;
    z-index: 1;
  }

  .cta-content h2 {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
  }

  .cta-content p {
    font-size: var(--font-size-xl);
    margin-bottom: var(--space-8);
    max-width: 100%;
  }

  .cta-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    margin-bottom: var(--space-6);
  }

  .cta-contact a {
    color: white;
    text-decoration: underline;
    font-weight: 600;
  }

  .cta-contact a:hover {
    opacity: 0.8;
  }

  @media (max-width: 1024px) {
    .hero .container {
      grid-template-columns: 1fr;
      gap: var(--space-12);
    }

    .hero-stats {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-4);
    }

    .hero-actions {
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: center;
    }

    .props-grid {
      grid-template-columns: 1fr;
    }

    .market-content {
      grid-template-columns: 1fr;
      gap: var(--space-12);
    }

    .market-stats {
      grid-template-columns: repeat(3, 1fr);
    }

    .evidence-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .cta-actions {
      flex-wrap: wrap;
    }
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: var(--font-size-4xl);
    }

    .hero-subtitle {
      font-size: var(--font-size-lg);
    }

    .section-header h2 {
      font-size: var(--font-size-3xl);
    }

    .section-header p {
      font-size: var(--font-size-xl);
    }

    .market-stats {
      grid-template-columns: 1fr;
    }

    .evidence-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding: var(--space-16) 0 var(--space-12);
    }

    .hero-title {
      font-size: var(--font-size-3xl);
    }

    .hero-subtitle {
      font-size: var(--font-size-base);
    }

    .hero-stats {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .hero-actions {
      flex-direction: column;
      width: 100%;
    }

    .btn-primary-large,
    .btn-secondary-large {
      width: 100%;
      text-align: center;
    }

    .agent-grid {
      grid-template-columns: 1fr;
    }

    .section-header h2 {
      font-size: var(--font-size-2xl);
    }

    .prop-card h3 {
      font-size: var(--font-size-xl);
    }

    .cta-content h2 {
      font-size: var(--font-size-2xl);
    }

    .cta-content p {
      font-size: var(--font-size-lg);
    }

    .cta-actions {
      flex-direction: column;
      width: 100%;
    }
  }
</style>
