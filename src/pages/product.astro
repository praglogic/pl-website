---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Lilo Solace | Pragmatic Logic" description="98.7% crisis detection accuracy. 100% recall. 178ms response time. Clinical-grade AI that prevents crises and saves lives.">
  <Navigation />

  <!-- Sticky Navigation -->
  <nav class="sticky-nav" id="stickyNav">
    <div class="container">
      <div class="nav-links">
        <a href="#overview">Overview</a>
        <a href="#agents">7 Agents</a>
        <a href="#roi">ROI</a>
        <a href="#roles">Who It's For</a>
        <a href="#infrastructure">Infrastructure</a>
        <a href="#demo">Demo</a>
      </div>
    </div>
  </nav>

  <!-- Hero Section - Human Story First -->
  <section class="apple-product-hero" id="overview">
    <div class="container">
      <div class="hero-content">
        <!-- Human Story Opening -->
        <div class="hero-story">
          <p class="story-text">"For the first time in months, Mom smiled when someone truly listened."</p>
          <p class="story-context">What if AI could provide the therapeutic connection every senior deserves?</p>
        </div>
        
        <!-- Product Introduction -->
        <div class="product-intro">
          <div class="product-badge">
            <span class="badge-icon">🎯</span>
            <span class="badge-text">Voice-First • Clinical-Grade • Enterprise Ready</span>
          </div>
          
          <h1 class="product-title">Lilo Solace</h1>
          <p class="product-tagline">Therapeutic AI that prevents crises<br>before they happen</p>
        </div>
        
        <!-- Key Proof Points -->
        <div class="proof-points">
          <div class="proof-primary">
            <div class="proof-number">98.7%</div>
            <div class="proof-label">Crisis Detection Accuracy</div>
            <div class="proof-detail">100% Recall • Zero False Negatives</div>
          </div>
          <div class="proof-supporting">
            <div class="proof-stat">
              <span class="stat-number">178ms</span>
              <span class="stat-label">Response Time</span>
            </div>
            <div class="proof-stat">
              <span class="stat-number">$3,000+</span>
              <span class="stat-label">Cost Savings</span>
            </div>
            <div class="proof-stat">
              <span class="stat-number">6:1</span>
              <span class="stat-label">ROI</span>
            </div>
          </div>
        </div>

        <!-- Primary Actions -->
        <div class="hero-actions">
          <a href="/demo" class="btn-primary-large">See Live Demo</a>
          <a href="#how-it-works" class="btn-secondary-large">How It Works</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ROI Section -->
  <section class="roi-section" id="roi">
    <div class="container">
      <div class="roi-hero">
        <h2 class="roi-headline">$500 per bed saves $3,000+<br><span class="roi-emphasis">in readmission costs.</span></h2>
        <p class="roi-tagline">6:1 return on investment with measurable outcomes</p>
      </div>

      <div class="roi-content">
        <div class="roi-calculator">
          <h3>ROI Calculator</h3>
          <div class="calculator-visual">
            <div class="cost-bar investment">
              <div class="bar-label">Monthly Investment</div>
              <div class="bar-amount">$500/bed</div>
              <div class="bar-visual" style="width: 16.7%;"></div>
            </div>
            <div class="cost-bar savings">
              <div class="bar-label">Crisis Prevention Savings</div>
              <div class="bar-amount">$3,000+/bed</div>
              <div class="bar-visual" style="width: 100%;"></div>
            </div>
          </div>
          <div class="roi-result">
            <span class="roi-multiple">6:1</span>
            <span class="roi-description">Return on Investment</span>
          </div>
          <a href="/contact" class="btn-primary">Calculate for Your Facility</a>
        </div>

        <div class="roi-benefits">
          <div class="benefit-item">
            <div class="benefit-icon">📉</div>
            <div class="benefit-content">
              <h4>40% Fewer Crisis Interventions</h4>
              <p>Prevent costly emergency responses and hospitalizations</p>
            </div>
          </div>
          <div class="benefit-item">
            <div class="benefit-icon">⏱️</div>
            <div class="benefit-content">
              <h4>2.5 Hours Staff Time Saved</h4>
              <p>Per resident per week through automated monitoring</p>
            </div>
          </div>
          <div class="benefit-item">
            <div class="benefit-icon">📊</div>
            <div class="benefit-content">
              <h4>Quality Metrics Improvement</h4>
              <p>Better CMS ratings and value-based care outcomes</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works - Progressive Disclosure -->
  <section class="how-it-works-section" id="how-it-works">
    <div class="container">
      <div class="how-hero">
        <h2 class="how-headline">Three Simple Steps.<br><span class="how-emphasis">Profound Impact.</span></h2>
        <p class="how-tagline">From conversation to crisis prevention in under 3 minutes</p>
      </div>

      <div class="how-journey">
        <div class="journey-step" data-step="1">
          <div class="step-visual">
            <div class="step-number">1</div>
            <div class="step-icon">🗣️</div>
          </div>
          <div class="step-content">
            <h3>Senior Speaks Naturally</h3>
            <p>Voice-first interface designed for 85% of seniors who prefer speaking over typing</p>
            <div class="step-details">
              <div class="detail-item">✓ 3-second pause tolerance</div>
              <div class="detail-item">✓ Hearing aid compatible</div>
              <div class="detail-item">✓ Medical vocabulary optimized</div>
            </div>
          </div>
        </div>

        <div class="journey-step" data-step="2">
          <div class="step-visual">
            <div class="step-number">2</div>
            <div class="step-icon">🧠</div>
          </div>
          <div class="step-content">
            <h3>AI Analyzes & Responds</h3>
            <p>7 therapeutic agents work together to provide clinical-grade support</p>
            <div class="step-details">
              <div class="detail-item">✓ Crisis detection (98.7% accuracy)</div>
              <div class="detail-item">✓ Therapeutic intervention</div>
              <div class="detail-item">✓ Emotional support</div>
            </div>
          </div>
        </div>

        <div class="journey-step" data-step="3">
          <div class="step-visual">
            <div class="step-number">3</div>
            <div class="step-icon">👥</div>
          </div>
          <div class="step-content">
            <h3>Care Team Alerted</h3>
            <p>Real-time notifications ensure the right person responds at the right time</p>
            <div class="step-details">
              <div class="detail-item">✓ Severity-based routing</div>
              <div class="detail-item">✓ SLA timer tracking</div>
              <div class="detail-item">✓ Family coordination</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Expandable Technical Details -->
      <div class="technical-disclosure">
        <button class="disclosure-toggle" onclick="toggleTechnicalDetails()">
          <span>View Technical Details</span>
          <span class="toggle-icon">↓</span>
        </button>
        <div class="technical-content" id="technicalContent">
          <div class="tech-grid">
            <div class="tech-item">
              <h4>Voice Processing</h4>
              <p>Whisper STT + Piper TTS<br>Open-source, $0 API costs</p>
            </div>
            <div class="tech-item">
              <h4>AI Architecture</h4>
              <p>26 microservices<br>Kubernetes-ready</p>
            </div>
            <div class="tech-item">
              <h4>Response Time</h4>
              <p>178ms average<br>Sub-100ms target</p>
            </div>
            <div class="tech-item">
              <h4>Compliance</h4>
              <p>HIPAA compliant<br>7-year audit logs</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 7 Agents -->
  <section class="agents-section">
    <div class="container">
      <div class="section-header">
        <h2>Seven Specialized Agents</h2>
        <p>Each agent is trained for specific clinical interventions, working together like a real care team.</p>
      </div>

      <div class="agents-grid">
        <div class="agent-card">
          <div class="agent-number">1</div>
          <h3>Behavioral Activation</h3>
          <p>Evidence-based depression treatment through personalized activity scheduling</p>
        </div>

        <div class="agent-card">
          <div class="agent-number">2</div>
          <h3>Cognitive Behavioral Therapy</h3>
          <p>Structured interventions for anxiety, mood disorders, and negative thought patterns</p>
        </div>

        <div class="agent-card">
          <div class="agent-number">3</div>
          <h3>Reminiscence Therapy</h3>
          <p>Memory-based interventions that improve mood and sense of identity</p>
        </div>

        <div class="agent-card">
          <div class="agent-number">4</div>
          <h3>Social Connection</h3>
          <p>Combating isolation through meaningful conversation and relationship building</p>
        </div>

        <div class="agent-card">
          <div class="agent-number">5</div>
          <h3>Mindfulness & Relaxation</h3>
          <p>Stress reduction and emotional regulation techniques</p>
        </div>

        <div class="agent-card">
          <div class="agent-number">6</div>
          <h3>Crisis Assessment</h3>
          <p>Real-time monitoring and immediate escalation for safety concerns</p>
        </div>

        <div class="agent-card">
          <div class="agent-number">7</div>
          <h3>Care Coordination</h3>
          <p>Orchestrates all agents and integrates with clinical care teams</p>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section class="how-section">
    <div class="container">
      <h2>How It Works</h2>

      <div class="how-grid">
        <div class="how-step">
          <div class="step-number">1</div>
          <h3>Continuous Engagement</h3>
          <p>Seniors interact naturally through conversation—no technical skills required. The system learns their unique patterns, preferences, and stories.</p>
        </div>

        <div class="how-step">
          <div class="step-number">2</div>
          <h3>Intelligent Routing</h3>
          <p>Our Multi-Agent Coordinator analyzes each interaction and routes it to the most appropriate therapeutic agent based on clinical need.</p>
        </div>

        <div class="how-step">
          <div class="step-number">3</div>
          <h3>Quality Assurance</h3>
          <p>Every response is evaluated across 7 dimensions of quality, ensuring clinical-grade accuracy and empathetic communication.</p>
        </div>

        <div class="how-step">
          <div class="step-number">4</div>
          <h3>Real-Time Monitoring</h3>
          <p>The Crisis Assessment agent continuously monitors for warning signs, alerting care teams immediately when intervention is needed.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Who It's For -->
  <section class="for-section">
    <div class="container">
      <h2>Who It's For</h2>

      <div class="for-grid">
        <div class="for-card">
          <h3>Healthcare Facilities</h3>
          <ul>
            <li>Reduce crisis interventions by 40%</li>
            <li>Improve quality metrics and outcomes</li>
            <li>Reduce staff burden and burnout</li>
            <li>Demonstrate measurable ROI</li>
          </ul>
          <a href="/contact" class="link-arrow">Learn More →</a>
        </div>

        <div class="for-card">
          <h3>Families</h3>
          <ul>
            <li>Peace of mind through continuous monitoring</li>
            <li>Stay connected to loved ones' daily lives</li>
            <li>Early warning of potential issues</li>
            <li>Dignified, respectful care</li>
          </ul>
          <a href="/contact" class="link-arrow">Learn More →</a>
        </div>

        <div class="for-card">
          <h3>Healthcare Systems</h3>
          <ul>
            <li>Scalable mental health support</li>
            <li>Value-based care alignment</li>
            <li>Clinical evidence and validation</li>
            <li>Regulatory compliance built-in</li>
          </ul>
          <a href="/contact" class="link-arrow">Learn More →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Voice-First Technology -->
  <section class="voice-section" id="voice">
    <div class="container">
      <div class="voice-hero">
        <h2 class="voice-headline">Voice-First by Design</h2>
        <p class="voice-tagline">85% of seniors prefer voice over text—we built our entire platform around this insight</p>
      </div>

      <div class="voice-content">
        <div class="voice-features">
          <div class="voice-feature">
            <div class="voice-icon">🎤</div>
            <h3>Open-Source Voice Stack</h3>
            <p>Whisper STT + Piper TTS = $0 API costs with unlimited conversations</p>
            <div class="voice-specs">
              <span class="spec-item">95%+ accuracy for senior voices</span>
              <span class="spec-item">Real-time processing</span>
            </div>
          </div>

          <div class="voice-feature">
            <div class="voice-icon">🧠</div>
            <h3>Voice Stress Detection</h3>
            <p>Vocal patterns enhance crisis detection beyond text analysis</p>
            <div class="voice-specs">
              <span class="spec-item">Tremor & pace analysis</span>
              <span class="spec-item">Emotional state recognition</span>
            </div>
          </div>

          <div class="voice-feature">
            <div class="voice-icon">♿</div>
            <h3>Senior-Optimized</h3>
            <p>Extended pause handling, hearing aid compatibility, medical vocabulary</p>
            <div class="voice-specs">
              <span class="spec-item">3-second pause tolerance</span>
              <span class="spec-item">Background noise filtering</span>
            </div>
          </div>
        </div>

        <div class="voice-demo">
          <div class="demo-flow">
            <div class="flow-step">
              <div class="step-icon">🗣️</div>
              <div class="step-label">Senior speaks</div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-step">
              <div class="step-icon">📝</div>
              <div class="step-label">Whisper transcribes</div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-step">
              <div class="step-icon">🤖</div>
              <div class="step-label">AI processes</div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-step">
              <div class="step-icon">🔊</div>
              <div class="step-label">Piper responds</div>
            </div>
          </div>
          <div class="demo-timing">Total response time: <strong>178ms</strong></div>
        </div>
      </div>
    </div>
  </section>

  <!-- Infrastructure & Scale -->
  <section class="infrastructure-section" id="infrastructure">
    <div class="container">
      <div class="infra-hero">
        <h2 class="infra-headline">Enterprise-Grade Infrastructure</h2>
        <p class="infra-tagline">Production-ready architecture that scales from pilot to nationwide deployment</p>
      </div>

      <div class="infra-content">
        <div class="infra-grid">
          <div class="infra-card">
            <div class="infra-icon">🏗️</div>
            <h3>26-Microservice Architecture</h3>
            <p>Kubernetes-ready, Docker-composed, with 13 active services and 13 ready for deployment</p>
            <div class="infra-details">
              <div class="detail-item">✓ AI Services (4)</div>
              <div class="detail-item">✓ Healthcare Dashboards (5)</div>
              <div class="detail-item">✓ Business Services (3)</div>
              <div class="detail-item">✓ Infrastructure (2)</div>
            </div>
          </div>

          <div class="infra-card">
            <div class="infra-icon">🔒</div>
            <h3>HIPAA Compliance</h3>
            <p>Built-in security with 7-year audit logging, end-to-end encryption, and role-based access</p>
            <div class="infra-details">
              <div class="detail-item">✓ JWT Authentication</div>
              <div class="detail-item">✓ PHI Detection</div>
              <div class="detail-item">✓ Audit Trails</div>
              <div class="detail-item">✓ RBAC System</div>
            </div>
          </div>

          <div class="infra-card">
            <div class="infra-icon">🔗</div>
            <h3>EHR Integration</h3>
            <p>HL7 FHIR R4 compatible with Epic, Oracle Health, and Cerner systems</p>
            <div class="infra-details">
              <div class="detail-item">✓ SMART on FHIR</div>
              <div class="detail-item">✓ LOINC Codes</div>
              <div class="detail-item">✓ Assessment Sync</div>
              <div class="detail-item">✓ Clinical Notes</div>
            </div>
          </div>

          <div class="infra-card">
            <div class="infra-icon">📊</div>
            <h3>Performance & Scale</h3>
            <p>Sub-100ms response times with capacity for 10,000+ concurrent users</p>
            <div class="infra-details">
              <div class="detail-item">✓ 99.99% Uptime SLA</div>
              <div class="detail-item">✓ Auto-scaling</div>
              <div class="detail-item">✓ Load Balancing</div>
              <div class="detail-item">✓ Disaster Recovery</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>See Lilo Solace in Action</h2>
        <p>
          Schedule a personalized demo to see how our 7-agent system delivers clinical-grade therapeutic care.
        </p>

        <div class="cta-actions">
          <a href="/demo" class="btn-primary-large">Request Demo</a>
          <a href="/evidence" class="btn-secondary-large">View Clinical Evidence</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Demo Video Modal -->
  <div id="demoModal" class="demo-modal">
    <div class="modal-content">
      <div class="modal-header">
        <h3>Lilo Solace Demo Preview</h3>
        <button class="modal-close" onclick="closeDemoModal()">&times;</button>
      </div>
      <div class="modal-body">
        <div class="video-placeholder">
          <div class="video-content">
            <div class="placeholder-icon">🎬</div>
            <h4>Demo Video Coming Soon</h4>
            <p>We're preparing a comprehensive 3-minute demo showcasing:</p>
            <ul class="demo-features">
              <li>✓ Real-time crisis detection in action</li>
              <li>✓ Family coordination and alerts</li>
              <li>✓ Therapeutic intervention examples</li>
              <li>✓ 7-agent system workflow</li>
            </ul>
            <div class="placeholder-actions">
              <a href="/demo" class="btn-primary">Schedule Live Demo</a>
              <button onclick="closeDemoModal()" class="btn-secondary">Close</button>
            </div>
          </div>
          <!-- Replace this placeholder with actual video when ready:
          <video controls width="100%" height="400">
            <source src="/videos/lilo-solace-demo.mp4" type="video/mp4">
            Your browser does not support the video tag.
          </video>
          -->
        </div>
      </div>
    </div>
  </div>

  <Footer />
</Layout>

<script>
  function openDemoModal() {
    document.getElementById('demoModal').style.display = 'flex';
    document.body.style.overflow = 'hidden';
  }

  function closeDemoModal() {
    document.getElementById('demoModal').style.display = 'none';
    document.body.style.overflow = 'auto';
  }

  // Close modal when clicking outside
  window.onclick = function(event) {
    const modal = document.getElementById('demoModal');
    if (event.target === modal) {
      closeDemoModal();
    }
  }

  // Close modal with Escape key
  document.addEventListener('keydown', function(event) {
    if (event.key === 'Escape') {
      closeDemoModal();
    }
  });

  // Technical Details Toggle
  function toggleTechnicalDetails() {
    const content = document.getElementById('technicalContent');
    const toggle = document.querySelector('.disclosure-toggle');
    
    if (content.classList.contains('expanded')) {
      content.classList.remove('expanded');
      toggle.classList.remove('active');
      toggle.querySelector('span').textContent = 'View Technical Details';
    } else {
      content.classList.add('expanded');
      toggle.classList.add('active');
      toggle.querySelector('span').textContent = 'Hide Technical Details';
    }
  }
</script>

<style>
  /* Sticky Navigation */
  .sticky-nav {
    position: sticky;
    top: 0;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--color-border-light);
    z-index: 100;
    transition: all 0.3s ease;
  }

  .nav-links {
    display: flex;
    gap: var(--space-8);
    justify-content: center;
    padding: var(--space-4) 0;
  }

  .nav-links a {
    color: var(--color-text);
    text-decoration: none;
    font-weight: 500;
    font-size: var(--font-size-sm);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-lg);
    transition: all 0.2s ease;
  }

  .nav-links a:hover {
    background: var(--color-background-soft);
    color: var(--color-primary);
  }

  /* Apple Product Hero */
  .apple-product-hero {
    padding: var(--space-20) 0;
    background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%);
    text-align: center;
  }

  .product-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.1), rgba(16, 185, 129, 0.05));
    border: 2px solid rgba(37, 99, 235, 0.2);
    border-radius: var(--radius-full);
    padding: var(--space-3) var(--space-6);
    margin-bottom: var(--space-6);
    font-weight: 600;
    color: var(--color-primary);
    font-size: var(--font-size-sm);
  }

  .badge-icon {
    font-size: 1.125rem;
  }

  /* Hero Story Section */
  .hero-story {
    text-align: center;
    margin-bottom: var(--space-12);
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .story-text {
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 500;
    color: var(--color-text);
    line-height: 1.3;
    margin-bottom: var(--space-4);
    font-style: italic;
  }

  .story-context {
    font-size: clamp(1.125rem, 2.5vw, 1.375rem);
    color: var(--color-text-light);
    font-weight: 400;
    line-height: 1.4;
  }

  /* Product Introduction */
  .product-intro {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .product-title {
    font-size: clamp(3.5rem, 8vw, 5rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    margin-bottom: var(--space-3);
    color: var(--color-text);
    background: linear-gradient(135deg, var(--color-text) 0%, var(--color-primary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .product-tagline {
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    color: var(--color-text);
    margin-bottom: var(--space-4);
    font-weight: 600;
    line-height: 1.2;
  }

  /* Proof Points */
  .proof-points {
    margin-bottom: var(--space-12);
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .proof-primary {
    text-align: center;
    margin-bottom: var(--space-8);
    padding: var(--space-8);
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.05), rgba(16, 185, 129, 0.03));
    border-radius: var(--radius-2xl);
    border: 2px solid rgba(37, 99, 235, 0.1);
  }

  .proof-number {
    font-size: clamp(3rem, 6vw, 4rem);
    font-weight: 700;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: var(--space-2);
  }

  .proof-label {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--color-text);
    font-weight: 600;
    margin-bottom: var(--space-2);
  }

  .proof-detail {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    font-weight: 500;
  }

  .proof-supporting {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-4);
  }

  .proof-stat {
    text-align: center;
    padding: var(--space-6);
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
  }

  .proof-stat:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .stat-number {
    display: block;
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-1);
  }

  .stat-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    font-weight: 500;
  }

  /* How It Works Section */
  .how-it-works-section {
    padding: var(--space-24) 0;
    background: linear-gradient(180deg, var(--color-background-soft) 0%, white 100%);
  }

  .how-hero {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .how-headline {
    font-size: clamp(2.5rem, 4.5vw, 3.5rem);
    font-weight: 600;
    line-height: 1.1;
    margin-bottom: var(--space-4);
    color: var(--color-text);
    letter-spacing: -0.025em;
  }

  .how-emphasis {
    display: inline-block;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 700;
  }

  .how-tagline {
    font-size: clamp(1.125rem, 2vw, 1.375rem);
    color: var(--color-text-light);
    font-weight: 400;
    font-style: italic;
  }

  .how-journey {
    display: grid;
    gap: var(--space-12);
    margin-bottom: var(--space-16);
  }

  .journey-step {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-8);
    align-items: start;
    padding: var(--space-8);
    background: white;
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-sm);
    transition: all 0.4s ease;
  }

  .journey-step:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .step-visual {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-4);
  }

  .step-number {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: var(--font-size-xl);
  }

  .step-icon {
    font-size: 3rem;
  }

  .step-content h3 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: var(--space-3);
  }

  .step-content p {
    color: var(--color-text-light);
    margin-bottom: var(--space-4);
    line-height: 1.6;
  }

  .step-details {
    display: grid;
    gap: var(--space-2);
  }

  .detail-item {
    font-size: var(--font-size-sm);
    color: var(--color-secondary);
    font-weight: 600;
  }

  /* Technical Disclosure */
  .technical-disclosure {
    text-align: center;
  }

  .disclosure-toggle {
    background: transparent;
    border: 2px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-4) var(--space-8);
    font-size: var(--font-size-base);
    font-weight: 600;
    color: var(--color-text);
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
  }

  .disclosure-toggle:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .toggle-icon {
    transition: transform 0.3s ease;
  }

  .disclosure-toggle.active .toggle-icon {
    transform: rotate(180deg);
  }

  .technical-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.5s ease;
    margin-top: var(--space-8);
  }

  .technical-content.expanded {
    max-height: 500px;
  }

  .tech-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-6);
    padding: var(--space-8);
    background: var(--color-background-soft);
    border-radius: var(--radius-xl);
  }

  .tech-item {
    text-align: center;
  }

  .tech-item h4 {
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: var(--space-2);
  }

  .tech-item p {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    line-height: 1.5;
  }

  .hero-metrics {
    margin-bottom: var(--space-12);
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .metric-primary {
    text-align: center;
    margin-bottom: var(--space-8);
    padding: var(--space-8);
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.05), rgba(16, 185, 129, 0.03));
    border-radius: var(--radius-2xl);
    border: 2px solid rgba(37, 99, 235, 0.1);
  }

  .metric-number {
    font-size: clamp(3rem, 6vw, 4rem);
    font-weight: 700;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: var(--space-2);
  }

  .metric-label {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--color-text);
    font-weight: 600;
  }

  .metric-secondary {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-4);
  }

  .metric-item {
    text-align: center;
    padding: var(--space-6);
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
  }

  .metric-item:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .metric-value {
    display: block;
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-1);
  }

  .metric-desc {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    font-weight: 500;
  }

  .hero-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: var(--space-12);
  }

  .btn-tertiary {
    padding: var(--space-3) var(--space-6);
    background: transparent;
    color: var(--color-text-light);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    font-weight: 500;
    font-size: var(--font-size-base);
    transition: all 0.3s ease;
  }

  .btn-tertiary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .demo-teaser {
    max-width: 600px;
    margin: 0 auto;
  }

  .demo-preview {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-6);
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.05), rgba(16, 185, 129, 0.02));
    border: 1px solid rgba(16, 185, 129, 0.2);
    border-radius: var(--radius-xl);
    transition: all 0.3s ease;
    cursor: pointer;
  }

  .demo-preview:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .demo-icon {
    font-size: 2rem;
    flex-shrink: 0;
  }

  .demo-text h4 {
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: var(--space-1);
  }

  .demo-text p {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    margin: 0;
  }

  .demo-badge {
    background: var(--color-primary);
    color: white;
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-full);
    font-size: var(--font-size-sm);
    font-weight: 600;
    margin-left: auto;
  }

  .demo-preview.coming-soon {
    background: linear-gradient(135deg, rgba(107, 114, 128, 0.05), rgba(107, 114, 128, 0.02));
    border: 1px solid rgba(107, 114, 128, 0.2);
    opacity: 0.8;
  }

  .demo-preview.coming-soon:hover {
    opacity: 1;
    transform: translateY(-1px);
  }

  .coming-soon-badge {
    background: var(--color-text-light) !important;
    color: white !important;
  }

  /* Demo Modal */
  .demo-modal {
    display: none;
    position: fixed;
    z-index: 1000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(5px);
    align-items: center;
    justify-content: center;
    animation: fadeIn 0.3s ease;
  }

  .modal-content {
    background: white;
    border-radius: var(--radius-2xl);
    max-width: 800px;
    width: 90%;
    max-height: 90vh;
    overflow-y: auto;
    box-shadow: var(--shadow-xl);
    animation: slideInUp 0.3s ease;
  }

  .modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-6) var(--space-8);
    border-bottom: 1px solid var(--color-border-light);
  }

  .modal-header h3 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
  }

  .modal-close {
    background: none;
    border: none;
    font-size: 2rem;
    color: var(--color-text-light);
    cursor: pointer;
    padding: 0;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-lg);
    transition: all 0.2s ease;
  }

  .modal-close:hover {
    background: var(--color-background-soft);
    color: var(--color-text);
  }

  .modal-body {
    padding: var(--space-8);
  }

  .video-placeholder {
    text-align: center;
  }

  .video-content {
    padding: var(--space-12);
    background: linear-gradient(135deg, var(--color-background-soft), white);
    border-radius: var(--radius-xl);
    border: 2px dashed var(--color-border);
  }

  .placeholder-icon {
    font-size: 4rem;
    margin-bottom: var(--space-6);
  }

  .video-content h4 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: var(--space-4);
  }

  .video-content p {
    font-size: var(--font-size-lg);
    color: var(--color-text-light);
    margin-bottom: var(--space-6);
  }

  .demo-features {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-8) 0;
    text-align: left;
    max-width: 400px;
    margin-left: auto;
    margin-right: auto;
  }

  .demo-features li {
    padding: var(--space-3) 0;
    color: var(--color-text);
    font-weight: 500;
    border-bottom: 1px solid var(--color-border-light);
  }

  .demo-features li:last-child {
    border-bottom: none;
  }

  .placeholder-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary, .btn-secondary {
    padding: var(--space-3) var(--space-6);
    border-radius: var(--radius-lg);
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    border: 2px solid transparent;
    cursor: pointer;
    font-size: var(--font-size-base);
  }

  .btn-primary {
    background: var(--color-primary);
    color: white;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .btn-secondary {
    background: transparent;
    color: var(--color-text);
    border-color: var(--color-border);
  }

  .btn-secondary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes slideInUp {
    from { 
      opacity: 0;
      transform: translateY(30px);
    }
    to { 
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Platform Overview Section */
  .platform-overview {
    padding: var(--space-24) 0;
    background: white;
  }

  .platform-hero {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .platform-headline {
    font-size: clamp(2.5rem, 4.5vw, 3.5rem);
    font-weight: 600;
    line-height: 1.1;
    margin-bottom: var(--space-4);
    color: var(--color-text);
    letter-spacing: -0.025em;
  }

  .platform-emphasis {
    display: inline-block;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 700;
  }

  .platform-tagline {
    font-size: clamp(1.125rem, 2vw, 1.375rem);
    color: var(--color-text-light);
    font-weight: 400;
    font-style: italic;
  }

  .platform-matrix {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-8);
  }

  .platform-card {
    padding: var(--space-10);
    background: linear-gradient(135deg, var(--color-background-soft), white);
    border-radius: var(--radius-2xl);
    border: 2px solid var(--color-border-light);
    transition: all 0.4s ease;
    text-align: center;
  }

  .platform-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-xl);
    border-color: var(--color-primary);
  }

  .platform-icon {
    font-size: 3rem;
    margin-bottom: var(--space-4);
  }

  .platform-card h3 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: var(--space-3);
  }

  .platform-users {
    font-size: var(--font-size-sm);
    color: var(--color-primary);
    font-weight: 600;
    margin-bottom: var(--space-6);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .platform-features {
    margin-bottom: var(--space-6);
  }

  .feature-item {
    padding: var(--space-2) 0;
    font-size: var(--font-size-sm);
    color: var(--color-text);
    border-bottom: 1px solid var(--color-border-light);
  }

  .feature-item:last-child {
    border-bottom: none;
  }

  .platform-specs {
    font-size: var(--font-size-xs);
    color: var(--color-text-light);
    font-weight: 500;
    font-style: italic;
  }

  /* Voice Section */
  .voice-section {
    padding: var(--space-24) 0;
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.05), rgba(16, 185, 129, 0.03));
  }

  .voice-hero {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .voice-headline {
    font-size: clamp(2.5rem, 4.5vw, 3.5rem);
    font-weight: 600;
    line-height: 1.1;
    margin-bottom: var(--space-4);
    color: var(--color-text);
    letter-spacing: -0.025em;
  }

  .voice-tagline {
    font-size: clamp(1.125rem, 2vw, 1.375rem);
    color: var(--color-text-light);
    font-weight: 400;
  }

  .voice-content {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--space-16);
    align-items: start;
  }

  .voice-features {
    display: grid;
    gap: var(--space-8);
  }

  .voice-feature {
    padding: var(--space-8);
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
  }

  .voice-feature:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
  }

  .voice-icon {
    font-size: 2.5rem;
    margin-bottom: var(--space-4);
  }

  .voice-feature h3 {
    font-size: var(--font-size-xl);
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: var(--space-3);
  }

  .voice-feature p {
    color: var(--color-text-light);
    margin-bottom: var(--space-4);
    line-height: 1.6;
  }

  .voice-specs {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .spec-item {
    font-size: var(--font-size-sm);
    color: var(--color-secondary);
    font-weight: 600;
  }

  .voice-demo {
    background: white;
    padding: var(--space-8);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-lg);
    text-align: center;
  }

  .demo-flow {
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr auto 1fr;
    align-items: center;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .flow-step {
    text-align: center;
  }

  .step-icon {
    font-size: 2rem;
    margin-bottom: var(--space-2);
  }

  .step-label {
    font-size: var(--font-size-sm);
    color: var(--color-text);
    font-weight: 500;
  }

  .flow-arrow {
    color: var(--color-primary);
    font-weight: 700;
    font-size: 1.5rem;
  }

  .demo-timing {
    font-size: var(--font-size-lg);
    color: var(--color-text-light);
    padding: var(--space-4);
    background: var(--color-background-soft);
    border-radius: var(--radius-lg);
  }

  .demo-timing strong {
    color: var(--color-secondary);
    font-weight: 700;
  }

  /* Infrastructure Section */
  .infrastructure-section {
    padding: var(--space-24) 0;
    background: white;
  }

  .infra-hero {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .infra-headline {
    font-size: clamp(2.5rem, 4.5vw, 3.5rem);
    font-weight: 600;
    line-height: 1.1;
    margin-bottom: var(--space-4);
    color: var(--color-text);
    letter-spacing: -0.025em;
  }

  .infra-tagline {
    font-size: clamp(1.125rem, 2vw, 1.375rem);
    color: var(--color-text-light);
    font-weight: 400;
  }

  .infra-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-8);
  }

  .infra-card {
    padding: var(--space-10);
    background: linear-gradient(135deg, var(--color-background-soft), white);
    border-radius: var(--radius-2xl);
    border: 2px solid var(--color-border-light);
    transition: all 0.4s ease;
  }

  .infra-card:hover {
    transform: translateY(-6px);
    box-shadow: var(--shadow-xl);
    border-color: var(--color-primary);
  }

  .infra-icon {
    font-size: 3rem;
    margin-bottom: var(--space-4);
  }

  .infra-card h3 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: var(--space-3);
  }

  .infra-card p {
    color: var(--color-text-light);
    margin-bottom: var(--space-6);
    line-height: 1.6;
  }

  .infra-details {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-3);
  }

  .detail-item {
    font-size: var(--font-size-sm);
    color: var(--color-secondary);
    font-weight: 600;
  }

  /* ROI Section */
  .roi-section {
    padding: var(--space-24) 0;
    background: linear-gradient(180deg, var(--color-background-soft) 0%, white 100%);
  }

  .roi-hero {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .roi-headline {
    font-size: clamp(2.5rem, 4.5vw, 3.5rem);
    font-weight: 600;
    line-height: 1.1;
    margin-bottom: var(--space-4);
    color: var(--color-text);
    letter-spacing: -0.025em;
  }

  .roi-emphasis {
    display: inline-block;
    background: linear-gradient(135deg, var(--color-secondary), var(--color-primary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 700;
  }

  .roi-tagline {
    font-size: clamp(1.125rem, 2vw, 1.375rem);
    color: var(--color-text-light);
    font-weight: 400;
    font-style: italic;
  }

  .roi-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: start;
  }

  .roi-calculator {
    background: white;
    padding: var(--space-10);
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-lg);
  }

  .roi-calculator h3 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: var(--space-8);
    text-align: center;
  }

  .calculator-visual {
    margin-bottom: var(--space-8);
  }

  .cost-bar {
    margin-bottom: var(--space-6);
  }

  .bar-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    margin-bottom: var(--space-2);
  }

  .bar-amount {
    font-size: var(--font-size-xl);
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: var(--space-3);
  }

  .bar-visual {
    height: 12px;
    border-radius: var(--radius-lg);
    transition: all 0.3s ease;
  }

  .investment .bar-visual {
    background: linear-gradient(90deg, #ef4444, #f97316);
  }

  .savings .bar-visual {
    background: linear-gradient(90deg, var(--color-secondary), var(--color-primary));
  }

  .roi-result {
    text-align: center;
    padding: var(--space-6);
    background: var(--color-background-soft);
    border-radius: var(--radius-xl);
    margin-bottom: var(--space-6);
  }

  .roi-multiple {
    display: block;
    font-size: var(--font-size-4xl);
    font-weight: 700;
    color: var(--color-secondary);
    margin-bottom: var(--space-2);
  }

  .roi-description {
    font-size: var(--font-size-base);
    color: var(--color-text-light);
    font-weight: 500;
  }

  .roi-benefits {
    display: grid;
    gap: var(--space-6);
  }

  .benefit-item {
    display: flex;
    align-items: start;
    gap: var(--space-4);
    padding: var(--space-6);
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
  }

  .benefit-item:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .benefit-icon {
    font-size: 2rem;
    flex-shrink: 0;
  }

  .benefit-content h4 {
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: var(--space-2);
  }

  .benefit-content p {
    font-size: var(--font-size-base);
    color: var(--color-text-light);
    margin: 0;
    line-height: 1.6;
  }

  /* Hero Section */
  .product-hero {
    padding: var(--space-24) 0 var(--space-20);
    background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%);
    text-align: center;
  }

  .hero-content {
    max-width: 900px;
    margin: 0 auto;
  }

  .product-hero h1 {
    font-size: clamp(3rem, 8vw, 4.5rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    margin-bottom: var(--space-4);
    color: var(--color-text);
  }

  .hero-subtitle {
    font-size: clamp(1.125rem, 3vw, 1.5rem);
    color: var(--color-text-light);
    line-height: 1.6;
    margin-bottom: var(--space-12);
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-8);
    margin-bottom: var(--space-12);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .stat {
    text-align: center;
  }

  .stat-number {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-2);
  }

  .stat-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    font-weight: 500;
  }

  .hero-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Section Headers */
  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto var(--space-16);
  }

  .section-header h2 {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
    color: var(--color-text);
    letter-spacing: -0.02em;
  }

  .section-header p {
    font-size: var(--font-size-xl);
    color: var(--color-text-light);
    line-height: 1.7;
  }

  .lead {
    font-size: var(--font-size-xl);
    color: var(--color-text);
    line-height: 1.7;
    margin-bottom: var(--space-8);
  }

  /* What Section */
  .what-section {
    padding: var(--space-24) 0;
    background: white;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-8);
  }

  .feature {
    padding: var(--space-8);
    background: var(--color-background-soft);
    border-radius: var(--radius-2xl);
    transition: all 0.3s ease;
  }

  .feature:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: var(--space-4);
  }

  .feature h3 {
    font-size: var(--font-size-xl);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--color-text);
  }

  .feature p {
    color: var(--color-text);
    line-height: 1.8;
    margin: 0;
    max-width: 100%;
    font-size: var(--font-size-base);
  }

  /* Agents Section */
  .agents-section {
    padding: var(--space-24) 0;
    background: var(--color-background-soft);
  }

  .agents-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-6);
  }

  .agent-card {
    padding: var(--space-6);
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
    position: relative;
  }

  .agent-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .agent-number {
    position: absolute;
    top: var(--space-4);
    right: var(--space-4);
    width: 32px;
    height: 32px;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: var(--font-size-sm);
  }

  .agent-card h3 {
    font-size: var(--font-size-lg);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--color-text);
    padding-right: var(--space-8);
  }

  .agent-card p {
    color: var(--color-text);
    font-size: var(--font-size-base);
    line-height: 1.7;
    margin: 0;
    max-width: 100%;
  }

  /* How Section */
  .how-section {
    padding: var(--space-24) 0;
    background: white;
  }

  .how-section h2 {
    text-align: center;
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-16);
    color: var(--color-text);
  }

  .how-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-8);
  }

  .how-step {
    position: relative;
    padding-left: var(--space-12);
  }

  .step-number {
    position: absolute;
    left: 0;
    top: 0;
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: var(--font-size-xl);
  }

  .how-step h3 {
    font-size: var(--font-size-xl);
    font-weight: 700;
    margin-bottom: var(--space-3);
    color: var(--color-text);
  }

  .how-step p {
    color: var(--color-text);
    line-height: 1.8;
    margin: 0;
    max-width: 100%;
    font-size: var(--font-size-base);
  }

  /* For Section */
  .for-section {
    padding: var(--space-24) 0;
    background: var(--color-background-soft);
  }

  .for-section h2 {
    text-align: center;
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-16);
    color: var(--color-text);
  }

  .for-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-8);
  }

  .for-card {
    padding: var(--space-8);
    background: white;
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
  }

  .for-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .for-card h3 {
    font-size: var(--font-size-2xl);
    font-weight: 700;
    margin-bottom: var(--space-6);
    color: var(--color-text);
  }

  .for-card ul {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6) 0;
  }

  .for-card li {
    padding: var(--space-2) 0;
    color: var(--color-text-light);
    line-height: 1.6;
    position: relative;
    padding-left: var(--space-6);
  }

  .for-card li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-secondary);
    font-weight: 700;
  }

  .link-arrow {
    color: var(--color-primary);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.2s ease;
  }

  .link-arrow:hover {
    gap: var(--space-2);
  }

  /* Tech Section */
  .tech-section {
    padding: var(--space-24) 0;
    background: white;
  }

  .tech-content {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--space-16);
    align-items: start;
  }

  .tech-text h2 {
    font-size: var(--font-size-4xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
    color: var(--color-text);
  }

  .tech-features {
    display: grid;
    gap: var(--space-6);
    margin-top: var(--space-8);
  }

  .tech-feature h4 {
    font-size: var(--font-size-lg);
    font-weight: 600;
    margin-bottom: var(--space-2);
    color: var(--color-text);
  }

  .tech-feature p {
    color: var(--color-text);
    line-height: 1.7;
    margin: 0;
    max-width: 100%;
    font-size: var(--font-size-base);
  }

  .tech-stats {
    position: sticky;
    top: var(--space-8);
    background: var(--color-background-soft);
    padding: var(--space-8);
    border-radius: var(--radius-2xl);
    display: grid;
    gap: var(--space-6);
  }

  .tech-stat {
    text-align: center;
    padding: var(--space-4);
  }

  .tech-stat-number {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-2);
  }

  .tech-stat-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
  }

  /* CTA Section */
  .cta-section {
    padding: var(--space-20) 0;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    text-align: center;
  }

  .cta-content h2 {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    margin-bottom: var(--space-4);
  }

  .cta-content p {
    font-size: var(--font-size-xl);
    margin-bottom: var(--space-8);
    opacity: 0.95;
    max-width: 100%;
  }

  .cta-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary-large, .btn-secondary-large {
    padding: var(--space-4) var(--space-8);
    border-radius: var(--radius-xl);
    font-weight: 600;
    font-size: var(--font-size-lg);
    text-decoration: none;
    transition: all 0.3s ease;
    border: 2px solid transparent;
  }

  .btn-primary-large {
    background: white;
    color: var(--color-primary);
  }

  .btn-primary-large:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .btn-secondary-large {
    background: transparent;
    color: white;
    border-color: rgba(255, 255, 255, 0.5);
  }

  .btn-secondary-large:hover {
    border-color: white;
    background: rgba(255, 255, 255, 0.1);
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .nav-links {
      gap: var(--space-4);
    }

    .nav-links a {
      font-size: var(--font-size-xs);
      padding: var(--space-1) var(--space-3);
    }

    .metric-secondary {
      grid-template-columns: 1fr;
      gap: var(--space-3);
    }

    .roi-content {
      grid-template-columns: 1fr;
      gap: var(--space-12);
    }

    .platform-matrix {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .voice-content {
      grid-template-columns: 1fr;
      gap: var(--space-12);
    }

    .demo-flow {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .flow-arrow {
      transform: rotate(90deg);
    }

    .infra-grid {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .infra-details {
      grid-template-columns: 1fr;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .how-grid {
      grid-template-columns: 1fr;
    }

    .for-grid {
      grid-template-columns: 1fr;
    }

    .tech-content {
      grid-template-columns: 1fr;
    }

    .tech-stats {
      position: relative;
      top: 0;
    }
  }

  @media (max-width: 768px) {
    .hero-stats {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .agents-grid {
      grid-template-columns: 1fr;
    }

    .how-step {
      padding-left: var(--space-16);
    }
  }

  @media (max-width: 640px) {
    .product-hero {
      padding: var(--space-16) 0;
    }

    .hero-actions {
      flex-direction: column;
      width: 100%;
    }

    .btn-primary-large,
    .btn-secondary-large {
      width: 100%;
    }
  }
</style>
